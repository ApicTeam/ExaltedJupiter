#include "game.h"
#include "minilib.h"

void HeroMove(SDL_Scancode key, int state)
{
    switch (key)
    {
        case SDL_SCANCODE_W:
            break;

        case SDL_SCANCODE_A:
            break;

        case SDL_SCANCODE_S:
            break;

        case SDL_SCANCODE_D:
            break;

        /* Just for handel all keys */
        default:
            break;
    }
}